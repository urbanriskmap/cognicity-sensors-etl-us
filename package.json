{
  "name": "sensor-lambda-us",
  "version": "0.0.1",
  "description": "Lambda ETL service for inserting, fetching water sensors data for RiskMap US deployments",
  "main": "index.js",
  "scripts": {
    "lint": "eslint src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./bin && ./node_modules/.bin/babel src -d bin",
    "deploy": "rm -rf ./bin && ./node_modules/.bin/babel src -d bin && serverless deploy -v"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/urbanriskmap/sensor-lambda-us.git"
  },
  "author": "Mayank Ojha",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/urbanriskmap/sensor-lambda-us/issues"
  },
  "homepage": "https://github.com/urbanriskmap/sensor-lambda-us#readme",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-istanbul": "^4.1.4",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.8.0",
    "eslint-config-google": "^0.9.1"
  },
  "dependencies": {
    "cors": "^2.8.1",
    "dotenv": "^4.0.0",
    "request": "^2.83.0",
    "request-promise-native": "^1.0.5"
  }
}
